import React from 'react';

const PageTemplate = ({children}) => (
    <div>
        <main>
            PageTemplate
            {children}
        </main>
    </div>
);

export default PageTemplate;
